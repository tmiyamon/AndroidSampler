<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tmiyamon.androidsampler.login">

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher">
        <activity android:name="com.tmiyamon.androidsampler.login.MainActivity"> </activity>

        <!-- Authenticator related activities and service needs to be declared here in order for the app to recognize them-->
        <activity android:name="com.tmiyamon.androidsampler.login.AuthenticatorActivity" android:label="@string/login_label"/>
        <activity android:name="com.tmiyamon.androidsampler.login.SignUpActivity" android:label="@string/signup_title"/>
        <service android:name=".AuthenticatorService">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>
            <meta-data android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>

    </application>

    <!-- client -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS"/>

    <!-- Authenticator -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS"/>

</manifest>
